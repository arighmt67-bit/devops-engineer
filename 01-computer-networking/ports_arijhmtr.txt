# /etc/apache2/ports.conf
# Apache2 listening on port 8080 instead of 80

Listen 8080

<IfModule ssl_module>
    Listen 443
</IfModule>

<IfModule mod_gnutls.c>
    Listen 443
</IfModule>

